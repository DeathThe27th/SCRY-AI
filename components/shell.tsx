"use client";
import Link from "next/link";import { usePathname } from "next/navigation";import { Menu,Search } from "lucide-react";import { Logo } from "./logo";
export function Shell({children}:{children:React.ReactNode}){const p=usePathname();return <><header className="topbar"><Link href="/"><Logo/></Link><label className="global-search"><Search size={15}/><input placeholder="Search ticker or company" aria-label="Search ticker or company"/></label><nav><Link className={p==="/"||p.startsWith("/stocks")?"active":""} href="/">Desk</Link><Link className={p==="/decisions"?"active":""} href="/decisions">Decisions</Link><Link className={p==="/methodology"?"active":""} href="/methodology">Methodology</Link></nav><div className="session"><i/> US CLOSED · TOKENIZED 24/7</div><span className="fresh">SNAPSHOT · AUG 27</span><button className="icon" aria-label="Open settings"><Menu size={17}/></button></header>{children}</>}
